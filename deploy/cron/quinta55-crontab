# No default schedule here -- unlike the trading instance's periodic
# screener (which the plan specifies a cadence for in Section 1/5),
# Quinta55's cadence depends entirely on workflows that aren't defined
# yet (see TODO in quinta55/CLAUDE.md). Once Diego specifies e.g. "daily
# inventory check" or "weekly supplier follow-up," add a line here of the
# same shape as the trading instance's:
#
# 0 8 * * 1-5 cd /opt/ecosystem/quinta55-repo && /usr/bin/docker compose run --rm hermes-chain --task "..." >> /opt/ecosystem/quinta55/logs/cron.log 2>&1
