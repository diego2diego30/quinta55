[project]
name = "quinta55-instance"
version = "0.1.0"
description = "Diego, Inc. -- Hermes orchestrator for the Quinta55/business instance"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
